[package]
name = "zero-core"
version = "0.1.0"
edition = "2021"

[dependencies]
zero-crypto = { path = "../zero-crypto" }
zero-dht = { path = "../zero-dht" }
zero-net = { path = "../zero-net" }
zero-onion = { path = "../zero-onion" }
zero-session = { path = "../zero-session" }
zero-offload = { path = "../zero-offload" }
tokio = { workspace = true }
bincode = { workspace = true }
tracing = { workspace = true }
thiserror = { workspace = true }
rand = { workspace = true }
